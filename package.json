{
  "name": "startup-dataset",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "A dataset of real Y Combinator companies' publicly documented technical stacks — databases, frameworks, hosting, and migration history — used as ground truth for evaluating why AI coding agents do or don't recommend a given database.",
  "scripts": {
    "validate": "tsx src/cli.ts validate"
  },
  "dependencies": {
    "yargs": "^17.7.3",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "^26.1.2",
    "@types/yargs": "^17.0.35",
    "tsx": "^4.23.1",
    "typescript": "^7.0.2"
  }
}
